---
description: 后端实现规范（API/文档解析/问答链路/配置）
globs: backend/**
alwaysApply: false
---

# Backend 规范（智能文档问答助手）

## 技术边界
- 后端默认 Python 实现，提供清晰 REST API。
- 核心能力必须覆盖：文档上传解析、文档查询删除、问答、多轮上下文、模型配置。
- 保持前后端分离，后端仅通过 API 暴露能力。

## 分层与职责
- 路由层：参数校验、响应结构，不承载业务细节。
- 服务层：文档处理、问答编排、上下文管理、提供商路由。
- 基础设施层：存储、外部 AI 客户端、配置加载。
- 禁止将业务逻辑散落在路由或配置文件中。

## 文档处理与问答约定
- 先实现 `TXT`、`Markdown` 解析与结构化存储。
- 问答链路要可追踪（输入、检索片段、模型响应、错误信息）。
- 多轮对话需要会话标识与上下文窗口策略，避免无限增长。

## 提供商与配置管理
- AI 提供商使用统一抽象接口，便于 OpenAI/Claude/本地模型切换。
- 关键配置通过环境变量或配置文件注入，禁止硬编码密钥。
- 提供商异常要转换为稳定、可读的 API 错误响应。

## 质量与可运维性
- 为核心逻辑提供单元测试：解析器、问答服务、配置加载、提供商适配层。
- 提供健康检查与基础日志，便于部署与问题定位。
- 必须可构建可运行 Docker 镜像，并在文档中给出启动方式。

## 文档要求
- 接口变更需同步更新接口文档（请求/响应/错误码）。
- 架构变更需同步更新架构文档，说明模块职责与调用关系。
