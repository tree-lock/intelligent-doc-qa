---
description: 前端实现规范（React/页面/交互/API 调用）
globs: frontend/**
alwaysApply: false
---

# Frontend 规范（智能文档问答助手）

## 技术边界
- 前端以 React + TypeScript 为默认实现。
- 页面至少覆盖：文档管理、Agent 问答、系统配置。
- 保持 UI 风格统一，组件优先复用，避免页面级重复逻辑。

## 目录与组件约定
- 页面容器与通用组件分离（如 `pages/` 与 `components/`）。
- 可复用状态逻辑抽到 hooks（如 `useDocuments`、`useChat`）。
- 工具函数放在 `lib/`，避免在组件中堆积复杂流程。

## 数据与接口约定
- 所有后端交互统一走 API 层封装，组件中不直接写裸 `fetch`。
- 请求状态需显式处理：`loading`、`success`、`error`。
- 对用户可见错误给出明确提示，不静默失败。

## 交互与可用性
- 文档上传操作要有进度/状态反馈。
- 问答页支持多轮上下文展示，区分用户与助手消息。
- 配置页支持模型提供商切换与关键参数校验。

## 质量要求
- 新增核心 UI 逻辑需配套单元测试（至少覆盖 hooks 或关键组件行为）。
- 样式变更不得破坏主流程可用性（上传、提问、配置保存）。

## 交付要求
- 前端变更需同步更新对应使用文档或界面说明。
- 与后端字段变更相关时，必须同步更新 API 文档与类型定义。
