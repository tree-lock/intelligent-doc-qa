# 复制为 .env 后按需修改，不要提交 .env 到 Git
# 用于 docker compose 及 Zeabur 等部署

# 后端 API 的完整地址（浏览器可访问），前端构建时会写入，无末尾斜杠
VITE_API_BASE_URL=https://doc-qa-api.zeabur.app

# 允许的前端来源，JSON 数组；若前后端同域填该域名即可
CORS_ALLOWED_ORIGINS=["https://doc-qa-api.zeabur.app"]
