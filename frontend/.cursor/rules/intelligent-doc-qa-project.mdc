---
description: 智能文档问答助手项目级约束与交付标准
alwaysApply: true
---

# 智能文档问答助手（项目级规则）

## 目标

- 构建一个可运行的 AI 文档问答系统，支持用户上传文档并通过自然语言提问获取准确答案。
- 默认以“可运行、可测试、可扩展”为实现优先级。

## 核心功能（必须实现）

1. 文档管理
   - 支持上传并解析 `TXT`、`Markdown` 文档。
   - 支持文档查询、删除。
2. 问答能力
   - 用户从已经上传的文档中勾选文档。
   - 基于文档内容生成准确、连贯答案。
   - 支持多轮对话，上下文连续。
   - 开启对话之后，不再允许减少文档内容，但允许再勾选新的文档。
3. 系统管理
   - 支持切换或配置 AI 提供商（如 OpenAI、Claude、本地模型）。
4. 前端页面
   - 至少提供：文档管理页、Agent 问答页、系统配置页。
   - UI 风格统一，组件设计可复用、便于演进。

## 技术与架构要求

- 采用前后端分离架构。
- 前端使用 React/Vue 等现代框架；后端使用 Python/Go 等语言。
- 代码设计优先模块化与清晰边界（文档层、检索/问答层、配置层、接口层）。

## 质量与工程要求（必须满足）

- 为核心逻辑提供单元测试（至少覆盖文档解析、问答链路关键逻辑、配置加载）。
- 提供可构建并可运行的容器镜像（Docker）。
- AI API 选型可自由，但需可配置、可替换。
- 提供完整文档：
  - 架构设计文档
  - 使用说明文档
  - 接口文档

## 加分项（条件允许时实现）

- 支持更多文档格式（如 PDF、Word）。

## 实现优先级指引

1. 先保证主链路可跑通：上传 -> 解析存储 -> 提问 -> 回答。
2. 再完善多轮对话、配置管理、UI 体验。
3. 最后扩展更多文档格式与高级能力。

## 执行约束

- 新增功能时必须说明其对应的需求条目（核心功能/非功能/加分项）。
- 任何改动不得破坏“可运行镜像 + 单元测试 + 关键文档”三项基础交付。
